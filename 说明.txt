teacher_study.system.sql是数据库文件。里面数据库关系都写好了。
core文件夹下面的是数据库操作的文件，子文件夹table里面是对应到表的数据操作函数。
logic里面是些逻辑的地方。
template里面是头，尾页的文件。
其他每个页面都在根目录下。

前面加index的首页的页面。
没有加的是后台页面。


需要做的是把登陆，注册功能做了，注册页面是registration.php

登陆页面是login.php

登陆登出的逻辑卸载logic里面吧。


还有就是把资源管理功能写一下，主要就是资源的上传，下载，列表显示。

最后就是在编辑教师页面，数据已经读过来了，但是点保存的时候存不到数据库。这个问题